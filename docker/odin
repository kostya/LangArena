FROM benchmarks:base

ARG version
RUN wget --progress=dot:giga -O - \
    https://github.com/odin-lang/Odin/releases/download/dev-$version/odin-linux-amd64-dev-$version.tar.gz \
    | tar -xz
ENV PATH="odin-linux-amd64-nightly+$version:${PATH}"
