FROM benchmarks:java

ARG version
RUN wget https://github.com/oracle/graalpython/releases/download/graal-$version/graalpy-$version-linux-amd64.tar.gz \
	&& tar -xzvf graalpy-$version-linux-amd64.tar.gz \
    && rm graalpy-$version-linux-amd64.tar.gz

ENV PATH="/opt/graalpy-$version-linux-amd64/bin:$PATH"
