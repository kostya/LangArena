FROM benchmarks:base

ARG version

RUN apt install -y --no-install-recommends \
    gcc-$version \
    g++-$version \
    && apt clean

RUN update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-$version $version \
    && update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-$version $version
