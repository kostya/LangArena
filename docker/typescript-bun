FROM benchmarks:base

# ============================================
# Установка Bun
# ============================================
ARG version

# Bun имеет официальный установщик
RUN curl -fsSL https://bun.sh/install | bash -s "bun-v${version}" \
    && ln -sf /root/.bun/bin/bun /usr/local/bin/bun

