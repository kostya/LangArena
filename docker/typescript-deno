FROM benchmarks:base

ARG version
RUN wget https://github.com/denoland/deno/releases/download/v$version/deno-x86_64-unknown-linux-gnu.zip \
    && unzip deno-x86_64-unknown-linux-gnu.zip \
    && rm deno-x86_64-unknown-linux-gnu.zip

ENV PATH="/opt:$PATH"
